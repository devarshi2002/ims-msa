pipeline {
    agent any

    environment {
        DOCKERHUB_CREDENTIALS = credentials('dockerhub')
        DOCKERHUB_REPO = 'devarshi2002/ims-msa-jenkins'
        COMPOSE_PROJECT_NAME = 'ims'
    }

    tools {
        maven 'Maven3'
        jdk 'Jdk'
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/devarshi2002/ims-msa.git'
            }
        }

        stage('Build Backend JAR Files') {
            steps {
                dir('Backend') {
                    script {
                        echo "🔨 Building all microservice JARs..."
                        bat '''
                        for /D %%d in (*) do (
                            if exist "%%d\\pom.xml" (
                                echo 🔨 Building %%d microservice...
                                pushd "%%d"
                                call mvn clean package -DskipTests
                                popd
                            )
                        )
                        '''
                    }
                }
            }
        }

        stage('Build Docker Images (Backend)') {
            steps {
                dir('Backend') {
                    script {
                        echo "🐳 Building all backend service images using docker-compose..."
                        bat 'docker-compose build'
                    }
                }
            }
        }

        stage('Build Frontend Image') {
            steps {
                dir('Frontend/ims') {
                    script {
                        echo "🌐 Building frontend Docker image..."
                        // Ensure Dockerfile exists before build
                        bat '''
                        if exist Dockerfile (
                            docker build -t %DOCKERHUB_REPO%/frontend:latest .
                        ) else (
                            echo ❌ ERROR: No Dockerfile found in Frontend/ims
                            exit /b 1
                        )
                        '''
                    }
                }
            }
        }

        stage('Tag Docker Images') {
            steps {
                script {
                    echo "🏷️ Tagging backend images..."
                    // Show current Docker images
                    bat 'docker images'

                    def services = ['apigateway', 'eureka', 'product', 'supplier', 'stock', 'order']
                    for (service in services) {
                        bat "docker tag ims-${service}:latest %DOCKERHUB_REPO%/${service}:latest"
                    }
                }
            }
        }

        stage('Push Images to DockerHub') {
            steps {
                script {
                    echo "📤 Logging in to DockerHub..."
                    bat 'echo %DOCKERHUB_CREDENTIALS_PSW% | docker login -u %DOCKERHUB_CREDENTIALS_USR% --password-stdin'

                    def services = ['apigateway', 'eureka', 'product', 'supplier', 'stock', 'order']
                    for (service in services) {
                        echo "⬆️ Pushing image for ${service}..."
                        bat "docker push %DOCKERHUB_REPO%/${service}:latest"
                    }

                    echo "⬆️ Pushing frontend image..."
                    bat "docker push %DOCKERHUB_REPO%/frontend:latest"
                }
            }
        }

        stage('Cleanup Local Images') {
            steps {
                script {
                    echo "🧹 Cleaning up local images..."
                    bat 'docker system prune -f'
                }
            }
        }
    }

    post {
        success {
            echo "✅ All backend + frontend images built and pushed successfully!"
        }
        failure {
            echo "❌ Build failed. Please check logs in Jenkins console."
        }
    }
}
